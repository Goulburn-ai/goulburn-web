node_modules/

# Build artifacts - generated by build.js from src/pages + src/partials
public/*.html

# Environment / secrets (defensive — this static frontend has no server secrets,
# but never commit a local .env if one is ever added)
.env
.env.local
.env.*.local
